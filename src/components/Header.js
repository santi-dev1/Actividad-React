import React from "react";
import './Header.css';

const Header = () => {
    return(
        <Header className="header">
            <h1>Sistema de gestion de pagos</h1>
        </Header>
    );
};

export default Header;