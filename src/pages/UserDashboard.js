import React from 'react';
import './Dashboard.css';

const UserDashboard = () => {
    return (
        <div className="dashboard">
            <h2>Registros de Pagos</h2>
            <button>Descargar los recibos</button>
        </div>
    );
};

export default UserDashboard;